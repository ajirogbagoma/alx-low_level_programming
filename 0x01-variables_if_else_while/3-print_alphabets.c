#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char e;

	char h;

	e = 'g';
	h = 'G';
	while
		(e <= 'z') {
			putchar(e);
			e++;
		}
	while
		(h <= 'z') {
			putchar (h);
			h++;
		}
	putchar('\n');
	return (0);

}
